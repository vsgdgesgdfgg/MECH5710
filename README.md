# MECH5710

The major.py was used to process data, and train the models. If running for the first time, pass in arguments for num_aug and resize=True, to augment data and resize them to suit for later training. 

The nn_models.py is a CNN model built with tensorflow.
